# ixora-infra

Infrastructure-as-code for **Ixora**.

## OpenTofu / Terraform

- **Staging (DigitalOcean):** [`opentofu/staging/`](opentofu/staging/README.md) — VPC, managed Postgres, Spaces, App Platform (Laravel API + Nuxt static admin).

See each stack’s `README.md` for prerequisites, `tofu init` / `plan` / `apply`, and security notes.
